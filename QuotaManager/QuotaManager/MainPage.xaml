<Page
    x:Class="QuotaManager.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:QuotaManager.ViewModels"
    xmlns:views="using:QuotaManager.Views"
    mc:Ignorable="d">
    
    <Page.DataContext>
        <viewModels:MainAppViewModel/>
    </Page.DataContext>
    
    <Page.Resources>
        <DataTemplate x:Key="QuotasCreation" x:DataType="viewModels:QuotasCreationViewModel">
            <views:QuotasCreation/>
        </DataTemplate>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button Content="Create Quota" Command="{Binding ShowQuotasCreationCommand}"></Button>
        
        <ContentControl Content="{Binding CurrentView}"/>
    </Grid>
</Page>
